# GOAL

Make `AGENTS/projects/prosegur_GCP/` the primary local home for the Dataform / GA4 / Google Ads integration repository previously edited from `Programming/projects/prosegur_GCP/`.

## Scope

- Preserve the repository structure needed for review and git work.
- Keep the legacy copy only as temporary fallback during cutover.

## Success criteria

- Shared Dataform guidance can point to this AGENTS path as the primary local repository.
- Future repo changes can be made from this project path.
